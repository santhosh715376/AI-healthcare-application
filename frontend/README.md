# Frontend (Person A)

Website look, components, and pages.

## Standalone Execution

Run standalone with mock data:
```bash
npm run dev -- --mock
```

Points at `contracts/mock-data/*.json` until backend endpoints are live.
